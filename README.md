# bjchambers.github.io

Personal resume, served at https://bjchambers.github.io.

Plain static HTML + CSS — no build step. Edit `index.html` and `styles.css`, commit, push.

## Preview locally

```sh
python3 -m http.server 8000
# then open http://localhost:8000
```